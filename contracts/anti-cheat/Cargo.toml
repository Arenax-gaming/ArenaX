[package]
name = "anti-cheat"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = "23.5.2"
arenax-events = { path = "../arenax-events" }

[dev-dependencies]
soroban-sdk = { version = "23.5.2", features = ["testutils"] }
